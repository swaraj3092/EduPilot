
  # EduPilot(TenzorX)

  This is a code bundle for EduPilot(TenzorX). The original project is available at https://www.figma.com/design/0pg1RUC1ZwoKDjXvxM8f21/EduPilot-TenzorX-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  